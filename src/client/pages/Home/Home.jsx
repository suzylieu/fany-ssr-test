import React from "react";

const Home = () => {
  return (
    <div>
      <div>Homepage12</div>
      <button onClick={() => console.log("click me")}>click me</button>
    </div>
  );
};

export default Home;